Run `./scripts/dev.sh` in the background to start the development server with hot reload.

Use `run_in_background: true` when invoking the Bash tool.

Tell the user: "Dev server starting - the app should appear shortly."
