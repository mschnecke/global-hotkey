# Run lint-staged for JS/TS/Svelte files
npx lint-staged

# Format Rust code (auto-fix)
cargo fmt --manifest-path src-tauri/Cargo.toml

# Type check (catches errors that eslint misses)
npm run check || { echo "TypeScript/Svelte type check failed. Run 'npm run check' to see errors."; exit 1; }
